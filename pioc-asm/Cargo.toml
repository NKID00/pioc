[package]
name = "pioc-asm"
version = "0.1.0"
edition = "2024"

[dependencies]
pioc-core = { path = "../pioc-core", features = ["std"]}
nom = "8"
nom-language = "0.1"
thiserror = "2"
derive_more = { version = "2", features = ["deref", "deref_mut"] }
strum = { version = "0.28", features = ["derive"] }
tracing = "0.1"
